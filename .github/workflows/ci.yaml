name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Start containers
        run: |
          docker compose -f docker/test/compose.yaml up -d

      - name: Build deps
        run: |
          docker compose -f docker/test/compose.yaml exec app go mod download -x

      - name: Go vet
        run: |
          docker compose -f docker/test/compose.yaml exec app go vet ./...

      - name: Errcheck
        run: |
          docker compose -f docker/test/compose.yaml exec app errcheck --verbose ./cmd/... ./domain/... ./handler/...

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Start containers
        run: |
          docker compose -f docker/test/compose.yaml up -d

      - name: Build deps
        run: |
          docker compose -f docker/test/compose.yaml exec app go mod download -x

      - name: Wait database
        run: |
          docker compose -f docker/test/compose.yaml exec app dockerize -wait tcp://db:5432 -timeout 60s

      - name: Setup database
        run: |
          docker compose -f docker/test/compose.yaml exec app make setup

      - name: Run tests
        run: |
          docker compose -f docker/test/compose.yaml exec app go test -v -count=1 ./...
